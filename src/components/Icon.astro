---
interface Props {
  name: string;
  class?: string;
}

const { name, class: className = '' } = Astro.props;
---

<span class:list={['material-symbols-outlined inline-flex align-middle', className]} aria-hidden="true">{name}</span>
